<%= form_with(model: compra, local: true) do |form| %>
  <% if compra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(compra.errors.count, "error") %> prohibited this compra from being saved:</h2>

      <ul>
        <% compra.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :data %>
    <%= form.date_select :data %>
  </div>

    <%# <%= autocomplete_field :fornecedor, :nome,
      autocomplete_fornecedor_nome_path,
      :update_elements => {:id => "##{form_tag_id(f.object_name, :fornecedor_id)}"},
      :id => form_tag_id(f.object_name, :autocomplete),
      :value => f.object.fornecedor.try(:nome),
      :placeholder => "Nome do centro de custo",
      :class => "span-8" %>

  <div class="field">
    <%= form.autocomplete_field :nome, compras_autocomplete_fornecedor_nome_path %>
    <%= form.label :fornecedor_id %>
  </div>

  <div class="field">
    <%= form.label :observacao %>
    <%= form.text_area :observacao %>
  </div>

  <div class="field">
    <%= form.label :valor_total %>
    <%= form.text_field :valor_total %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
